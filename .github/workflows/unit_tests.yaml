name: Assemble, Detekt, Lint and Unit tests
on:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Assemble, detekt, lint and Unit tests
        run: ./gradlew clean assembleDebug detekt lint test
